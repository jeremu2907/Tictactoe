/*
    This file is to develop algorithm for large scale tic tac toe
 */

import java.util.ArrayList;
// import java.util.HashMap;
public class Logic {
    public static boolean logic(ArrayList<Integer> tokenData){
        // if(tokenData.size() < 3) return false;

        // HashMap<Integer, Integer> data = new HashMap<Integer, Integer>();
        //     // <Value, idx>
            
        // for(int i = 0; i < tokenData.size() - 3; i++){
        //     //Find target number from tokenData[i]
        //     int target = 15 - i;

        //     //User target to iterate hashmap
        //     for(int j = i + 1; j < tokenData.size(); j++){
        //         if(data.containsValue(target - tokenData.get(j)) == true)
        //             return true;
        //         else{
        //             data.put(j,tokenData.get(j));
        //         }
        //     }
        // }

        
        return true;
    }
}
